---
name: Deploy Infrastructure-Development

on: 
  workflow_dispatch:

jobs:
  call-infra:
    uses: ./.github/workflows/DeployInfra.yml
    with:
      environment: development
    secrets: inherit
...